# Credencial do admin enquanto o app roda só com armazenamento local.
# Depois que o Supabase for plugado, a autenticação passa a ser feita lá
# e estas variáveis deixam de ser usadas.
VITE_ADMIN_EMAIL=admin@rrovos.com.br
VITE_ADMIN_PASSWORD=rrovos123

# Preenchido quando o projeto do Supabase for criado.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
